<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	x:Class="com.ithiredguns.orlandocodecamp.TracksPage">
	<StackLayout Spacing="0">
		<AbsoluteLayout>
			<SearchBar x:Name="trackSearch" Placeholder="Search..." TextChanged="Handle_TextChanged" BackgroundColor="#000000" AbsoluteLayout.LayoutBounds="0, 0, 1, 40" AbsoluteLayout.LayoutFlags="PositionProportional,WidthProportional" />
		</AbsoluteLayout>
		<StackLayout BackgroundColor="#000000" Padding="5,0,5,0">
			 
				<ListView x:Name="lstTracks" 
					IsPullToRefreshEnabled="true" 
					Refreshing="Handle_Refreshing"
					ItemSelected="Handle_ItemSelected"
					ItemTapped="Handle_ItemTapped"
					>
					<ListView.ItemTemplate>
					<DataTemplate>
					 <TextCell Text="{Binding Name}" Detail="{Binding Room}"  />
					</DataTemplate>

					</ListView.ItemTemplate>
				</ListView>
			 
		</StackLayout>
	</StackLayout>
</ContentPage>
